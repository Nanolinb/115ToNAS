# 版本日志（CHANGELOG）

本项目遵循语义化版本。每次功能变更对应一个 Git 提交与版本号。

## v1.19.7 — 2026-07-28

**剧集播放列表简化为集数数字方框**

- 观影端详情弹窗的剧集列表改为纯集数圆角方框（01、02…按现有集数排布，位于封面简介下方），点击直接播放该集；管理端保留原信息密集视图（字幕状态/文件大小/搜索字幕按钮）不受影响
- 遥控器聚焦态：方框变近白亮灰 + 2px 橙色（--accent）外圈；弹窗打开时焦点自动落在第一集，方向键几何导航实测 01→04 逐格移动正常
- adb 截图验证：Fallout 第2季 8 个方框排列与聚焦高亮符合设计

## v1.19.6 — 2026-07-28

**7z 字幕包解包 + 「同季不同剧」错挂根治（超人事件）**

- **7z 解包**：引入开源 py7zr（已加 requirements.txt 并装入运行容器），字幕站大量 7z 打包的中文字幕终于可用；包内按集数挑文件，支持裸编号命名（季包里 `01.ass/02.ass` 按集数对齐）
- **新抓出一个深层 bug**：上版「同季季包 +30 分」不看剧名重合——subhd 搜 1923 返回 Superman.And.Lois.S01，零词元重合照样过门槛，7 集全挂成《超人和露易丝》S01E01 字幕（实锤：7 份文件 md5 全同）。现在季包匹配必须同时有剧名词元重合，裸季号零重合一律淘汰
- **宁缺毋错**：多集季包内认不出对应集数时整包拒收（之前取包内第一个文件 = 必然挂错）
- assrt 搜索每页 10 → 20 条（1923 S01E08 的唯一正确候选之前被分页截掉）
- 实测：1923 S01E08 挂上深影字幕组正确中英字幕（逐行内容核验）；E01-E07 两站均无源，保持诚实无字幕；单测 19 项全过（含超人案例回归）

## v1.19.5 — 2026-07-28

**字幕刮削纠错：不再把动漫/别剧的字幕挂到影片上 + UTF-16 乱码根治**

- **根因**：在线搜刮（assrt/subhd）按语言标签取第一条候选就下载，完全不看文件名相关性。实测 ONE PIECE 真人版 8 集里 7 集挂的是《海贼王》动漫第 1145 集字幕（且同一份内容被复制到每集名下）；Disclosure Day 挂的是《白宫群英》；1923 挂的是 1920 年代默片《Erdgeist》（把 1923 当年份匹配）；Spider-Noir 同一份字幕被复制成 29 份挂到全季
- **相关度门槛**：候选必须与视频文件名对应——剧集要求集数精确匹配（+100）或同季季包（+30），电影要求词元有重合；动漫同名单集、别季别集、电影挂剧集字幕一律淘汰。单测 21 项全过
- **zip/多文件字幕包按集选文件**：之前取包内第一个文件，整季打包时除第一集外全错；现在按 SxxExx 挑对应集
- **7z/rar/gzip 拒收**：无法解压的包不再当 .srt 存（1923 的 13MB 二进制垃圾就是这么来的）；HTML 错误页同样拒收；某条候选失败自动尝试下一条。代价：1923 的中文源全是 7z，目前诚实保持无字幕，不再给错字幕
- **UTF-16 乱码根治**：字幕服务统一走 BOM/编码探测（UTF-16/GBK/Big5 → UTF-8）。Spider-Noir 的用户字幕是 UTF-16LE，之前原样输出导致播放器端乱码；下载落盘前也先转码再判格式（UTF-16 的 ASS 不再被误判成 .srt）
- **音轨标记误判修复**：发行名里的 ITA.ENG 等音频语言标记不再被当成字幕语言（Gen V 的中英 .ass 之前被标成 English 轨）
- **全库清理**：删除 100 个错挂/重复/伪字幕文件，按修复后逻辑重刮并逐部抽查内容：Disclosure Day《揭秘日》、Fallout S02 全季、Gen V S02 全季、Spider-Noir 全季（中英）、ONE PIECE S02 全季（E02 在线源 + 其余用户字幕）、Pressure、灌篮高手均已验证为正确中文字幕；DB 字幕轨已全量刷新
- 扫描补刮不再要求 assrt token（subhd 免 token 也能兜底）

## v1.19.4 — 2026-07-27

**播放器底部条去「关闭」按钮 + 剧集列表遥控器焦点标识**

- 播放窗口底部条移除「关闭」按钮（容易被理解成只是收起底部条；退出播放统一用返回键，裸画面按返回会先弹「确认退出/取消」）
- 详情弹窗剧集列表整行成为遥控器焦点项：聚焦行玻璃高亮+白色内描边、集数标签加粗，一眼看清选到哪集；整行可点=直接播放该集（网页端同样生效），行内小三角按钮 TV 上隐藏不再单独抢焦点。adb 实测：方向键从弹窗关闭钮逐行下移到 S02E03，截图确认高亮清晰

## v1.19.3 — 2026-07-27

**详情弹窗收窄 + 图标豆腐块清剿**

- 详情弹窗（简介+播放列表）最大宽度从 880px 收窄到 60vw（电视 960 视口下约屏宽 3/5），弹窗可纵向滚动，剧集列表不再被 `overflow:hidden` 切断
- 三处关闭钮「✕」与页首「🎬」在老 WebView（Chrome 66）上渲染成豆腐块，全部换成内联 SVG（描边 X / 场记板），web 管理端同步替换

## v1.19.2 — 2026-07-27

**剧集分组改按文件夹：同目录 = 同一部剧**

- 分组基准从「TMDB id / 解析标题」改为「库内顶层文件夹」优先：同一季不会再被拆成多张卡。实测：ONE PIECE S02 之前 8 集被拆成 7+1 两张卡（1 集匹配上 TMDB、7 集没匹配上），「1923」之前被拆成 7 张碎卡（数字剧名被解析器当成年份、剧名错取成意大利语单集名），现在都各合为一张卡
- 卡片元数据「少数服从多数」：一组内取集数最多的那个 TMDB 匹配，部分集误匹配不拖累整组；整组未匹配时用文件夹名解析剧名兜底
- 其他既有卡片（Fallout S02、Spider-Noir S01、V世代 S02、电影）回归验证无影响

## v1.19.1 — 2026-07-27

**TV App 稳定性与遥控器体验修复 + 首页 UI 打磨**

- **播放几秒白屏退出（重点）**：logcat 实锤是 media3 在播放线程反射 `AudioTrack.getLatency`，触发小米 Android 6 的 ART native bug（`Class_getDeclaredMethodInternal` 里 SIGSEGV）直接杀进程。升级最新 media3 1.8.0 也有这处反射（拉包验证过），改为启动时主线程预热反射提前完成类链接。实测 4 轮播放+退出循环无崩溃，长期复发率待观察
- **Dolby Vision 不再一刀切**：设备有杜比认证解码器（极米 R10 等）走原生 DV；只有没有 DV 解码器的老机器（小米电视）才回退 HEVC 基础层。Fallout S02E02 实测 4K HDR 正常
- **遥控器逻辑**：控制条没亮时左右键第一下只唤出控制条，再按左右=±10 秒快进退；上下键在控制条亮时交给它导航（可下选进度条）；BACK 逐层退（菜单条→控制条→影片墙）；方向键下/MENU 唤底部菜单条。底部菜单条按钮选中变琥珀黄底黑字、按下暗一档
- **默认轨道**：音轨 英>日>中，字幕 中文（简中>繁中）；外挂字幕按文件名推断简/繁标签。实测 Disclosure Day 6 条字幕自动选中「中文字幕」
- **屏保**：播放中和 App 前台都常亮，小米广告屏保不再插队；退出 App 交还系统
- **首页 UI**：海报焦点去掉大圈光晕，改为放大 1.06 倍+贴边白框浮到最前；海报统一 2:3 裁剪（Chrome 66 不支持 aspect-ratio 的修复，之前海报高矮不一）；整体上移消除底部文字出屏；Hero 停止 8 秒自动轮播只随焦点切换；Hero 背景优先用 TMDB 横版剧照（backdrop，服务端已有缓存，存量条目点「刷新影片信息」补抓）；海报行超 7 部横向平滑滚动；「管理」齿轮豆腐块改内联 SVG
- **桌面 web 搜索/分类消失**：根因是各端缓存了旧版静态文件，版本号统一 bump 强制刷新
- **4K 播放 OOM 白屏（重点根因修复）**：Fallout S02E03 连崩 3 次的日志显示是 `OutOfMemoryError`（播放线程）——ExoPlayer 默认缓冲按 50 秒算，4K 高码率要吃几百 MB 堆，老电视默认堆仅 128MB。改为 24MB 硬顶 + 10~20 秒缓冲窗，并开 `largeHeap`。实测 4K 连播 200 秒+内存平稳（~164MB 无增长），连按快进不崩；之前的「屏保广告后白屏」也是崩溃后系统回桌面触发的连锁反应
- **退出播放确认**：裸画面（无菜单/控制条）按返回键弹「确认退出/取消」，默认焦点在「取消」防误触；adb 实测弹窗、焦点、取消回播全通过
- **字幕样式**：去掉纯黑底块，改白字黑描边（对画面遮挡更少），截图验证无黑块
- 已知：播放速度功能 TV App 里并不存在（之前看到的齿轮是旧版残留界面），无需移除

## v1.19.0 — 2026-07-27

**安卓 TV App：三段式首页 + 播放菜单条 + Dolby Vision 黑屏修复**（v1.18.1~v1.18.5 为 TV App 中间迭代，统一在此记录）

- **TV 首页三段式改版**（参照流媒体 TV 交互）：左栏「继续观看」（原生播放器续播存档，没看过则填最新录入，点击直接续播）；中部 Hero 大背景图展示当前选中影片（双层交叉淡入，8 秒无操作自动轮播前 5 部）；底部海报行 + 全部/电影/剧集页签过滤。实测：小米电视（MiTV4，Android 6）adb 截图确认三区渲染正确、焦点框几何导航正常
- **播放器底部菜单条**：遥控器 MENU 或方向键下 → 底部弹出半透明圆角条（音轨/字幕/选集[剧集才有]/关闭），左右键移动、确认即生效；返回或 6 秒无操作自动收起。方向键上唤出 ExoPlayer 原生控制器（播放/暂停/进度条坚持原生不自造）；控制器隐藏时左右键 = ±10 秒快进退
- **轨道选择单点生效**：音轨/字幕/选集弹窗改单选列表，选中立即生效关闭（不用再下选「确认」按钮）；字幕菜单第 0 项为「关闭字幕」，当前轨打 ✓。实测：adb 模拟遥控 MENU→右→确认→下→确认，字幕切换成功且播放器不退出
- **修复菜单条按键穿透竞态**：弹窗抢焦点前的按键会落到菜单条按钮上，偶发误点「关闭」直接退出播放（adb 复现两次）。现在弹窗打开即收起菜单条；菜单条 6 秒自动隐藏时焦点交还播放画面，确认键不会再点到隐形按钮。回归实测两轮通过
- **Dolby Vision P7 黑屏修复**：Fallout S02（dvhe.07.06）在小米电视上黑屏有声。自定义 MediaCodecSelector 把 dolby-vision 重定向到 HEVC 解码器（DV→HDR10 降级解基础层），画面声音均正常，用户实测确认
- **播放黑背景**：去掉系统灰边；退出 App 加确认弹窗防误触；详情弹窗 Apple TV 风格化
- 已知环境限制：小米电视 WebView 为 Chrome 66（禁用 ??/inset 等新语法）；小米桌面/广告偶尔抢前台，BACK + 重进即可

## v1.18.0 — 2026-07-27

**转码流音画同步修复 + 字幕模糊匹配/清洗 + 自动转码判断 + 续播提示优化**

- **音画同步（重点修复）**：根因是转码起流时视频整包拷贝只能落在关键帧（如 598.6s）、音频却能精确落点（600s），ffmpeg 封包时按各自起点归零时间轴 → 音画错开一个 GOP，拖多少次进度条都错同样的量。现在服务端先用 framemd5 实测视频真实落点，音频走第二输入从同一点起解码、再用 `asetpts` 平移时间轴对齐（不能用 `-itsoffset`，会让 accurate seek 的丢弃边界算错）；视频流仍原样拷贝，NAS 不做视频转码。新增 `/api/stream-prep` 端点，前端起流前拿对齐点同步进度条与字幕
- **字幕**：同目录模糊匹配自动挂载（`Show.S01E01.*.srt` 不同发布组命名也能认）；播放详情实时重扫新放入的字幕；ASS 覆写标签清洗（`{\1c&H0080FF&}` 这类乱码不再出现在字幕里）；转码流重起后字幕按 offset 整体平移
- **自动转码判断**：浏览器能直解的（如 Slam Dunk 的 MKV+AC3）直接原生播放，不再强制转码；EAC3/DTS/TrueHD 自动实时转 AAC，音量键不再灰色等手动切换
- **续播提示条**：加 × 关闭按钮，8 秒无操作自动收起
- HEVC 转码流加 `hvc1` 标签，帮助 Mac 端识别走硬解
- 观影页/管理页右下角版本角标，排障时一眼确认部署版本

## v1.17.2 — 2026-07-26

**转码进度条强化 + 弹窗背景图回滚**

- 转码播放的自绘进度条加粗加时间标（`1:23:45 / 2:04:02`），一眼可认出、可点选；总长改由 `/tracks` 端点直接下发（ffprobe 探测），进度条一打开就是全片长度，不等 metadata
- 实测：Slam Dunk 这类 MKV+AC3 影片在浏览器必须走转码（Chrome 解不了 MKV 容器和 AC3 音轨），点选跳转后半段后端响应 0.24s
- 详情弹窗背景图回滚为 200px 横条（左右撑满、取封面第 2~3 份、底部渐隐）；封面/简介保持上叠 60px，按钮与弹窗边界固定 18px，弹窗不再拉长出滚动条

## v1.17.1 — 2026-07-26

**详情弹窗：背景图取封面完整上半部分 + 按钮与边界固定间距**

- 弹窗顶部背景图窗口改为 4:3 宽高比（= 封面上半部分的宽高比），完整显示封面上半部分，不裁不拉，底部 fade out 渐隐效果保留
- 弹窗底部按钮排/剧集列表与弹窗边界保持固定 18px 距离，不再随内容多少贴边

## v1.17.0 — 2026-07-26

**转码播放：自绘全片时长进度条，任意点选跳转**

- 原生进度条在流式转码响应下只能拖到已缓冲的位置（Chrome 限制），现在转码播放时视频下方有一条自绘进度条，**始终展示整部影片时长**
- 点选行为：点在已缓冲区间内直接跳转（零开销）；点在缓冲区外，音频从点选位置实时重新转码起流，一两秒续播
- 转码播放时隐藏 Chrome 原生时间轴避免混淆；非转码播放（浏览器能直解的）仍用原生进度条，不受影响
- 续播、音轨切换的进度换算同步修正（重起流后内部时间从 0 起算，进度条按绝对位置显示）

## v1.16.5 — 2026-07-26

**详情弹窗背景图修正 + 封面变形修复**

- 上一版理解错了：封面背景是放在**弹窗内部顶部**（不是整个网页背景）。整页遮罩恢复原来的纯色模糊；弹窗内顶部背景图取封面从上至下 6 等分的第 2~3 份（约 1/6~1/2 处，人物面部/主视觉通常在这一段），裁切不拉伸
- 修复详情弹窗小封面被拉高变形（如 The First Slam Dunk）：flex 容器默认 stretch 会把只设宽度的 img 撑高，钉住 align-self 保持原始宽高比

## v1.16.3 — 2026-07-26

**修复：AAC 实时转码流进度条无法拖动**

- 根因：转码流用了 `empty_moov`，MP4 头时长为 0，浏览器把它当直播流（进度条锁死）。改为在 moov 头写入整部影片时长，进度条恢复完整长度
- 拖动行为：缓冲区内直接拖；拖出缓冲区按拖动时间点重新起流转码，一两秒续播（音频实时转码开销不变，视频流仍是原样拷贝）
- 电视/投影端（ExoPlayer 走同一转码通道）一并受益

## v1.16.2 — 2026-07-26

**详情弹窗的虚化背景改用影片封面**

- 点开影片详情时，弹窗外的遮罩背景取该影片封面的上半部分铺满全屏，模糊 + 压暗处理（海报是竖构图，上半部分当横向背景效果最好）；无封面时回到原来的纯色遮罩

## v1.16.1 — 2026-07-26

**续播：第二次打开影片询问「是否从上次的位置开始」**

- 播放中每 5 秒记录进度到浏览器本地（每台设备各记各的）；看到 30 秒以上再打开同一影片，视频上方弹出询问条：「继续播放」从上次位置续播（转码流按时间点重起），「从头开始」清零重看
- 快播完（剩不到 60 秒）自动清档，下次打开直接从开头播；关闭播放器、切换剧集时也会即时存档

## v1.16.0 — 2026-07-26

**多音轨影片：语言自动优选 + 网页端可切换音轨**

- `/api/media/{id}/tracks` 现在返回每条音轨的语言标签（ffprobe language/title，ISO 639 各写法归一）与 `preferred_audio` 优选结果
- 优选顺序：中文 → English → 日语 → 第 0 条（如 Spider-Noir 的 ita+eng 自动选 eng）；想改顺序只动 `_preferred_audio` 一处
- 播放页音轨下拉菜单升级为真切换：显示「音轨 N · 语言 / 编码」，Safari 走原生 audioTracks，Chrome/Edge 等走转码流换 `?a=N` 参数、从当前进度秒级续播；优选轨不是第 0 条时自动用转码流起播，不再只能听第 0 条
- 修复：`/tracks` 原本要求管理员登录，纯观影端（电视浏览器等无管理 Cookie）拿不到音轨信息，自动转码/优选都不生效——现已放开给观影端

## v1.15.0 — 2026-07-26

**浏览器解不了的音轨自动实时转码 AAC（视频流不重编码）**

- 播放页检测到音轨是 AC3/EAC3/DTS/TrueHD 等浏览器解不了的编码时，自动改走 `/api/stream/{id}?audio=aac`：ffmpeg 只把音轨实时转 AAC、视频流原样拷贝，网页内直接出声，不用再跳 IINA/外部播放器
- 转码流不支持字节 seek：拖出缓冲区时按时间点（?t=秒）重新起流，一两秒起播
- 实测 J4125 上 EAC3→AAC 转码速度 ≈12MB/s，远超 1080p 码率，CPU 占用低；客户端断开即杀 ffmpeg
- IINA/电视播放器按钮保留，想听原始音轨（Atmos 等）仍可用

## v1.14.3 — 2026-07-26

**季标规则调整**

- 剧集一律标注「第N季」（含只有第一季的情况，如 Spider-Noir 第1季），不再隐藏单季剧的第一季标

## v1.14.2 — 2026-07-26

**播放器按钮调整**

- 去掉「下载文件」（局域网千兆直连播放，下载多此一举）
- 剧集播放新增「上一集」「下一集」：当前为第一集时「上一集」置灰，最后一集时「下一集」置灰；电影/单集自动隐藏

## v1.14.1 — 2026-07-26

**影片墙刷新即校验文件现实**

- 修复：此前只在「扫描」时才校验文件是否存在，被搬走/删除（不经本应用）的条目会一直残留墙上且可播；现在每次刷新影库都会校验——文件已不存在，或指向路径不在指定电影/剧集目录内 → 立即标 missing 下墙；库目录挂载不可达时跳过校验防止误判

## v1.14.0 — 2026-07-26

**题材/年份过滤数据兜底：subhd 详情页一把抓（剧情+类型+年代）**

- TMDB 刮不到题材/年份时（华语片、剧集单集常见），扫描与「刷新影片信息」自动去 subhd 详情页取「类型」「年代」，豆瓣继续补简介
- 观影端懒补扩展：海报墙发现缺简介/题材/年份的条目后台批量补全（每轮最多 20 条），剧集同剧各集共享、逐字段只补空不覆盖
- 修复后「题材」「年份」过滤器有数据可用

## v1.13.0 — 2026-07-26

**影库剧集按季分组 + 季标**

- 同一部剧的不同季（季号取自视频文件名解析的 Sxx）在影片墙上各自成卡：多季都有时分别标「第一季/第二季…」，只有单季且非第一季时标「第N季」（如 Fallout 第二季），只有第一季时保持裸剧名不加标

## v1.12.1 — 2026-07-26

**详情页按钮更名**

- 「重新匹配信息」更名「刷新影片信息」，输入框提示改为「输入影片名称或关键字」，表意更准确

## v1.12.0 — 2026-07-26

**影库只收视频文件 + 文件管理与影片墙即时联动 + 播放器选集列表**

- 修复：115 网盘下载的字幕文件落进媒体目录后被自动扫描误收入影库，剧集选集列表把字幕当成可播放视频列出；扫描入口一律拒绝非视频文件，影库接口对历史脏数据也做防御过滤
- 文件管理移动/改名即时联动：视频搬进电影/剧集目录立即入库上墙（字幕等忽略）；搬出库目录或删除立即从影片墙移除——影库严格只收录指定目录内的文件
- 播放器新增「选集」：播放剧集时展开当前剧集的全部集列表，高亮当前集，点击即切换

## v1.11.0 — 2026-07-26

**简介兜底：TMDB 刮不到时去 subhd/豆瓣找同名影片摘录 + 海报墙缺简介懒补**

- 扫描与「重新匹配信息」时 TMDB 无简介（华语片常见）→ 自动去 subhd.cc（/d/ 影片页「剧情」完整版）→ 豆瓣搜索页（结果自带截断简介）兜底
- 观影端海报墙发现缺简介条目时后台批量补（每轮最多 20 条，一次只跑一轮），下一轮刷新即见，不拖慢首屏；剧集同剧各集共享同一简介
- subhd 搜索按片名精确匹配选条目（alt 中文名完全一致优先，否则退回首条），修复搜「灌篮高手」错配「大灌篮」
- 简介抓取失败隔 3 秒自动重试一轮，容忍 NAS 出口网络抖动

## v1.10.2 — 2026-07-26

**封面候选预取缓存 + 候选墙内搜索/更多图片 + 修复启动卡死**

- 打开详情弹窗时后台预取最多 5 张封面候选（subhd+豆瓣快速源，服务端缓存 10 分钟），点「更换封面」立即有候选可选
- 候选墙顶部新增关键词输入框（默认填文件名解析词，可改）+「搜索」按钮，以及「更多图片」按钮触发三源全量搜索（subhd 6 + 豆瓣 6 + 百度 12）
- 修复：下载器恢复任务时 `is_logged_in` 是同步网络调用且直接跑在事件循环里，115 API 不可达时会冻住整个进程、Web 服务起不来；改为 `asyncio.to_thread` 调用（与 cloud115 其它调用一致）

## v1.10.1 — 2026-07-26

**封面候选需确认后生效**

- 「更换封面」候选墙改为两步：点选候选图（高亮描边）→ 按「设为封面」确认才下载生效，避免误触即换

## v1.10.0 — 2026-07-26

**subhd 字幕源（免 token）+ 豆瓣/subhd 封面源 + 本地上传封面 + 搜索词取文件名解析**

- 字幕在线搜刮加入 subhd.cc 作为第二来源（公开站点、免 token、国内可直连）：assrt 下到的语言不再重复，缺的语言由 subhd 补齐；支持 zip 字幕包（取 srt/vtt 优先）与 srt/ass/vtt 格式识别、GBK/Big5 自动转 UTF-8；候选按与文件名的相关度排序（剧集集数不一致直接淘汰）
- 封面候选墙升级为三源合并：subhd（搜索结果页附带的豆瓣海报，无防盗链）→ 豆瓣电影搜索（m_ratio 540px，先种 bid cookie 过反爬）→ 百度图片；每张候选带来源角标；豆瓣图床有防盗链，显示走新增的 `/api/imgproxy`（仅白名单 doubanio.com / subhd.me，防 SSRF）
- 「更换封面」「重新匹配信息」的搜索词留空时自动从文件名解析关键信息（片名+年份），不再依赖已刮削的标题
- 详情页新增「上传封面」按钮：直接传本地图片（JPG/PNG/GIF/WEBP，魔数校验，≤15MB），剧集同剧自动共享

## v1.9.1 — 2026-07-26

**网盘字幕可勾选下载 + 本地字幕模糊匹配**

- 115 网盘页：字幕文件（.srt/.ass/.ssa/.sub/.vtt）现在可以直接勾选下载，单选字幕落到目标目录根
- 本地字幕匹配升级为两级：严格同名前缀优先，其次模糊匹配——去掉发布标签（1080p/BluRay/x265 等）后互为前缀即认为同片（如 `Movie.2020.1080p.BluRay.mkv` 配 `Movie.2020.zh.srt`）；剧集要求集数标记（S01E02/E02/第2集）一致才匹配，杜绝错挂别集字幕
- 同一语言多条候选时：严格同名 > 模糊匹配 > 可播格式（vtt/srt 优先于 ass）

## v1.9.0 — 2026-07-26

**下载任务复选批量操作：改存储位置 + 删除所选（含临时文件）**

- 任务列表新增复选框与全选，勾选状态在 2 秒轮询刷新中保留
- 批量改存储位置：排队/暂停/失败/已取消的任务可整体改到别的 NAS 目录（目录选择器选择，可现场新建文件夹）；有 `.part` 临时文件的一并搬走，断点续传进度不丢；下载中/已完成的自动跳过
- 批量删除所选：删除任务记录并把未完成的 `.part` 临时文件一并删除；已下载完成的文件不受影响；下载中的任务跳过

## v1.8.0 — 2026-07-26

**文件管理页 + 下载任务批量操作 + 持久化防重复下载**

- 文件管理页（管理端新标签）：浏览 `/media` 下全部目录与文件，复选框多选 + 全选，支持新建文件夹、重命名、移动（单个或批量，目录选择器选目标）、删除（单个或批量，文件夹递归删除，二次确认）；删除/移动后自动同步媒体库与下载历史中的路径，观影端不出现失效条目
- 下载任务批量操作：下载页新增「全部暂停 / 全部启动 / 清空已下载」；全部启动会将已暂停/失败/已取消任务重新排队，清空已下载只删记录不动文件
- 防重复下载：新增 downloads 下载历史表（按 115 pickcode 持久记录已完成下载），即使「清空已下载」后再次勾选同一文件，只要文件仍在磁盘上即自动跳过并在 toast 中提示跳过数量；文件被人为删除后允许重新下载

## v1.7.0 — 2026-07-26

**115 字幕随片下载 + TV 按剧建文件夹 + IINA 一键拉起 + 浏览器无声检测**

- 字幕随片下载：整文件夹下载时同目录字幕一并入队；单选视频时自动去 115 同目录找同名字幕（`.srt/.ass/.ssa/.vtt/.sub`）。配对成功的字幕自动改名为 `<视频主干>.zh.srt` 这类带语言中缀的形式，扫描器直接识别；对不上的字幕不瞎下
- 目录结构还原：整文件夹下载按 115 原目录层级落盘（含根文件夹名）；单选剧集自动按解析出的剧名建文件夹，不再全部平铺。配合扫描器把 `poster.jpg` 落到视频同目录（Kodi/Plex 风格），字幕+封面+影片同一文件夹，方便管理
- IINA 一键拉起：点「🍎 用 IINA 打开」直接通过 `iina://open?url=` 拉起本地 IINA 播放签名链接，同时复制到剪贴板兜底（不再要求手动粘贴）
- 浏览器无声检测：音轨探测升级为返回音频编码列表（ffprobe `codec_type,codec_name`，注意 ffprobe csv 输出字段按字典序排列，已按位置无关方式解析）；遇到 eac3/ac3/dts 等浏览器解不了的编码时 toast 明确提示「会无声」并给出 IINA/外部播放器指引，Mac 上同时显示 IINA 按钮（即使容器是 mp4）
- 实测佐证：Spider-Noir 音轨为 eac3×2（DDP5.1），Chrome 不支持故无声；灌篮高手 ac3+mp3×2，Safari 可发声

## v1.6.0 — 2026-07-26

**签名播放链接（修外部播放器 401）+ 百度封面源 + 会话闲置超时**

- 签名播放链接：新增 HMAC 签名令牌（24 小时有效），「复制直链 / 下载文件 / IINA / 电视播放器」全部改用签名链接——修复设置观影密码后外部播放器拿不到 Cookie 导致 401 的问题；令牌随机密钥本地生成，可随时通过重置数据库失效
- IINA 按钮修复：不再跳 `iina://` 协议（未安装时 Safari 报「网址无效」），改为复制签名播放地址 + 操作指引（IINA → 文件 → 打开URL ⌘U）；Mac 全浏览器通用
- 封面新源（百度图片，国内直连）：① TMDB 刮不到封面时扫描自动兜底；② 详情页新增「更换封面」：百度搜图候选墙，点击即设定（剧集自动同剧共享）
- 会话闲置自动过期：管理端 12 小时无操作自动退出（滑动续期，正常使用不受影响），观影端 30 天；配合登录限流，局域网内即使被扫描也无法冒用会话

## v1.5.0 — 2026-07-25

**安全加固（防爆破）+ TMDB 直连优先/代理自动兜底 + Safari 播 MKV 指引**

安全：
- 登录限流：管理端登录、观影端登录、修改密码三个入口按来源 IP 计数，10 分钟内失败 5 次锁定 10 分钟（返回 429 与剩余时间），内存计数不耗 NAS 资源
- 密码哈希从 SHA-256 升级为 PBKDF2-HMAC-SHA256（26 万次迭代），旧格式验证通过后自动就地升级，无需重设密码
- 全部密码比较改为 hmac.compare_digest 常量时间比较，防时序侧信道
- 增加安全响应头：X-Content-Type-Options / X-Frame-Options / Referrer-Policy

TMDB 网络策略：
- 每次刮削先试直连，直连失败且配置了代理时自动回退代理（路由器/VPN 白名单放行 themoviedb.org 后无需任何配置）
- 连通结果（direct/proxy/fail + 时间）自动记录，管理端设置页代理栏实时显示；失败时代理输入框内浅红字提示（输入即覆盖）
- 每次下载完成自动刮削即自动重试一次，无需手动检测

观影体验：
- Mac Safari 打开 MKV/AVI 等不支持容器时，播放条显示「🍎 用 IINA 打开」按钮（iina:// URL scheme，IINA 硬解原码流、可切内嵌音轨字幕）

## v1.4.0 — 2026-07-25

**多语言字幕轨道 + 音轨菜单 + 管理端代理设置**

- 字幕升级为多语言轨道：简中 / English / 中英双语分别搜刮与识别（`*.zh.srt` / `*.en.srt` / `*.zh-en.srt`），本地已有字幕按中缀自动识别语言；缺哪种补哪种
- 播放器字幕切换：多轨道通过浏览器原生 CC 菜单切换
- 新增音轨菜单：ffprobe 探测内嵌音轨数；单音轨时菜单置灰；多音轨时仅 Safari 可网页内切换（Chrome/WebView 内核不开放该能力，菜单提示改用外部播放器/Infuse/IINA），镜像内置 ffmpeg
- 管理端设置页新增「网络代理」：仅 TMDB 刮削走代理（115 与字幕直连），解决 TMDB 国内不可直连导致的封面刮削失败；留空 = 直连
- 数据库自动迁移：media 表新增 subs 多轨道字段，旧单字幕记录无损迁移

## v1.3.0 — 2026-07-25

**安卓 TV 观影 App + 外部播放器硬解通道 + 遥控器导航 + 字幕修复**

- 新增 `android/` 原生观影 App（Java，零第三方依赖）：WebView 加载观影页海报墙，适配 Android TV 桌面（海信/极米等，LEANBACK_LAUNCHER + banner）；首次启动输入 NAS 地址，遥控器菜单键可重设；局域网 http 明文已放行
- 播放链路新增「外部播放器」通道：网页通过 JS 桥调起电视/投影仪自己的播放器（VLC / MX Player / Kodi / 系统播放器），由设备硬件解码，支持内嵌多音轨与字幕切换；画质 = 原文件码流（NAS 只做 Range 字节流，零转码零压缩）
- 观影页海报卡片支持遥控器方向键导航（可聚焦 + 聚焦高亮 + 确认键打开详情）
- GitHub Actions 自动编译 APK（push 到 main 即出包，Actions 页面下载 app-debug.apk 后可 U 盘/推送安装到电视）
- 修复字幕搜刮两个 bug：① 候选筛选误用搜索结果的 filelist（该字段只在 detail 接口返回）导致永远无候选；② 单文件字幕的下载地址在条目 url 字段而非 filelist，补充回退
- assrt 接口调用增加网络瞬断重试（3 次）与失败原因日志，不再静默失败

## v1.2.2 — 2026-07-25

**下载 403 修复 + 排序兜底 + 设置页防错位**

- 修复下载 403 invalid signature：取链时 UA 必须用 `user_agent` 形参传入（直链签名与取链 UA 绑定，f=1），之前误用 `headers` 传参导致取链 UA 与下载 UA 不一致，CDN 拒绝
- 网盘排序改为本地排序兜底，不再依赖 115 服务端排序参数；中文名称按拼音序（GBK 编码序），目录恒在文件前
- 设置页目录输入框标签改为各自独立、置于输入框上方，修复标签在框下方导致的「填串行」误解（电影/剧集/下载目录错位）

## v1.2.1 — 2026-07-25

**115 登录修复 + 网盘文件排序 + 稳定性**

- 修复扫码登录：完成登录通道从 `web` 换成 `alipaymini`（官方默认，`web` 易触发 115「IP登录异常」），URL 末尾斜杠与 POST 请求体对齐官方实现；扫码确认状态在服务端记忆，网络瞬断后自动续试，失败原因打日志
- 网盘文件列表支持排序：最新/最早、名称 A→Z/Z→A、大小大→小/小→大（服务端排序，目录恒在文件前）
- 文件列表与搜索增加网络瞬断重试（3 次）
- 管理端初始化/文件列表加载失败时显示错误与「重试」按钮，不再空白页；初始化失败 3 秒自动重试

## v1.2.0 — 2026-07-25

**安全加固 + 国内网络适配**

安全：
- 敏感信息（TMDB Key、assrt Token、115 Cookie）改为 AES-256-GCM 加密存储，密钥独立保存在 `secret.key`（权限 600），旧明文数据首次读取时自动迁移为密文
- 设置接口不再回传完整密钥：只返回掩码（`****后4位`）；前端原样回传掩码时服务端保留原值
- 115 Cookie 从明文文件迁移为加密入库，旧 `115-cookies.txt` 自动迁移后删除

网络适配（QNAP 实测）：
- 115 API 域名（qrcodeapi/webapi/proapi/userapi/passportapi/uplb）支持通过 docker-compose `extra_hosts` 固定到可达 IP（默认网关 IP 被封时可用，详见 DEPLOY-QNAP.md）
- 容器网络建议 `network_mode: host`，绕过 QTS 容器虚拟网关不稳定的问题
- 115 接口请求增加有限重试（二维码获取 5 次、状态轮询容错、登录完成 3 次），对抗家用网络瞬断
- 容器 DNS 可配置公共 DNS（223.5.5.5 / 119.29.29.29），规避运营商/Tailscale 劫持

## v1.1.0 — 2026-07-24

**双通道架构拆分：观影端与管理端分离**

- 新增观影端 `/`：海报墙 + 搜索/过滤 + 播放，默认免密，面向电视/投影仪/iPad
- 新增管理端 `/admin`：115 登录、下载队列、目录配置、密钥设置，独立管理密码
- 新增可选「观影密码」：设置后观影端需输密码，与管理密码互不影响
- 会话增加角色字段（admin/viewer），观影会话无法访问任何管理接口（已做越权测试）
- 观影端仅在管理端已登录时显示「搜索字幕 / 重新匹配」维护按钮
- 扫描媒体库入口从观影页移至管理端设置页

## v1.0.0 — 2026-07-24

**初始版本**

- 115 网盘：网页二维码扫码登录（p115client），网盘浏览/搜索/勾选下载，整文件夹递归加入队列
- 下载器：串行队列、断点续传、暂停/继续/取消、限速，下载完成自动刮削
- 媒体库：目录扫描 + guessit 文件名解析 + TMDB 海报/简介/评分/题材刮削，海报本地缓存
- 字幕：本地同名字幕识别 + assrt.net 在线中文字幕搜刮（.zh.srt）
- 播放：Range 流式播放（支持拖动），设备自解码，NAS 零转码；直链可复制到外部播放器
- Plex 式海报墙：关键字搜索 + 类型/题材/年份过滤，剧集按季分组
- 设置页网页化目录管理：浏览 NAS 目录、新建文件夹、指定电影/剧集/下载目录
- Docker 单容器部署（QNAP Container Station），限制 512MB 内存 / 1 核
