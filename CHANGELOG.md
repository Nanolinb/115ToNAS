# 版本日志（CHANGELOG）

本项目遵循语义化版本。每次功能变更对应一个 Git 提交与版本号。

## v1.5.0 — 2026-07-25

**安全加固（防爆破）+ TMDB 直连优先/代理自动兜底 + Safari 播 MKV 指引**

安全：
- 登录限流：管理端登录、观影端登录、修改密码三个入口按来源 IP 计数，10 分钟内失败 5 次锁定 10 分钟（返回 429 与剩余时间），内存计数不耗 NAS 资源
- 密码哈希从 SHA-256 升级为 PBKDF2-HMAC-SHA256（26 万次迭代），旧格式验证通过后自动就地升级，无需重设密码
- 全部密码比较改为 hmac.compare_digest 常量时间比较，防时序侧信道
- 增加安全响应头：X-Content-Type-Options / X-Frame-Options / Referrer-Policy

TMDB 网络策略：
- 每次刮削先试直连，直连失败且配置了代理时自动回退代理（路由器/VPN 白名单放行 themoviedb.org 后无需任何配置）
- 连通结果（direct/proxy/fail + 时间）自动记录，管理端设置页代理栏实时显示；失败时代理输入框内浅红字提示（输入即覆盖）
- 每次下载完成自动刮削即自动重试一次，无需手动检测

观影体验：
- Mac Safari 打开 MKV/AVI 等不支持容器时，播放条显示「🍎 用 IINA 打开」按钮（iina:// URL scheme，IINA 硬解原码流、可切内嵌音轨字幕）

## v1.4.0 — 2026-07-25

**多语言字幕轨道 + 音轨菜单 + 管理端代理设置**

- 字幕升级为多语言轨道：简中 / English / 中英双语分别搜刮与识别（`*.zh.srt` / `*.en.srt` / `*.zh-en.srt`），本地已有字幕按中缀自动识别语言；缺哪种补哪种
- 播放器字幕切换：多轨道通过浏览器原生 CC 菜单切换
- 新增音轨菜单：ffprobe 探测内嵌音轨数；单音轨时菜单置灰；多音轨时仅 Safari 可网页内切换（Chrome/WebView 内核不开放该能力，菜单提示改用外部播放器/Infuse/IINA），镜像内置 ffmpeg
- 管理端设置页新增「网络代理」：仅 TMDB 刮削走代理（115 与字幕直连），解决 TMDB 国内不可直连导致的封面刮削失败；留空 = 直连
- 数据库自动迁移：media 表新增 subs 多轨道字段，旧单字幕记录无损迁移

## v1.3.0 — 2026-07-25

**安卓 TV 观影 App + 外部播放器硬解通道 + 遥控器导航 + 字幕修复**

- 新增 `android/` 原生观影 App（Java，零第三方依赖）：WebView 加载观影页海报墙，适配 Android TV 桌面（海信/极米等，LEANBACK_LAUNCHER + banner）；首次启动输入 NAS 地址，遥控器菜单键可重设；局域网 http 明文已放行
- 播放链路新增「外部播放器」通道：网页通过 JS 桥调起电视/投影仪自己的播放器（VLC / MX Player / Kodi / 系统播放器），由设备硬件解码，支持内嵌多音轨与字幕切换；画质 = 原文件码流（NAS 只做 Range 字节流，零转码零压缩）
- 观影页海报卡片支持遥控器方向键导航（可聚焦 + 聚焦高亮 + 确认键打开详情）
- GitHub Actions 自动编译 APK（push 到 main 即出包，Actions 页面下载 app-debug.apk 后可 U 盘/推送安装到电视）
- 修复字幕搜刮两个 bug：① 候选筛选误用搜索结果的 filelist（该字段只在 detail 接口返回）导致永远无候选；② 单文件字幕的下载地址在条目 url 字段而非 filelist，补充回退
- assrt 接口调用增加网络瞬断重试（3 次）与失败原因日志，不再静默失败

## v1.2.2 — 2026-07-25

**下载 403 修复 + 排序兜底 + 设置页防错位**

- 修复下载 403 invalid signature：取链时 UA 必须用 `user_agent` 形参传入（直链签名与取链 UA 绑定，f=1），之前误用 `headers` 传参导致取链 UA 与下载 UA 不一致，CDN 拒绝
- 网盘排序改为本地排序兜底，不再依赖 115 服务端排序参数；中文名称按拼音序（GBK 编码序），目录恒在文件前
- 设置页目录输入框标签改为各自独立、置于输入框上方，修复标签在框下方导致的「填串行」误解（电影/剧集/下载目录错位）

## v1.2.1 — 2026-07-25

**115 登录修复 + 网盘文件排序 + 稳定性**

- 修复扫码登录：完成登录通道从 `web` 换成 `alipaymini`（官方默认，`web` 易触发 115「IP登录异常」），URL 末尾斜杠与 POST 请求体对齐官方实现；扫码确认状态在服务端记忆，网络瞬断后自动续试，失败原因打日志
- 网盘文件列表支持排序：最新/最早、名称 A→Z/Z→A、大小大→小/小→大（服务端排序，目录恒在文件前）
- 文件列表与搜索增加网络瞬断重试（3 次）
- 管理端初始化/文件列表加载失败时显示错误与「重试」按钮，不再空白页；初始化失败 3 秒自动重试

## v1.2.0 — 2026-07-25

**安全加固 + 国内网络适配**

安全：
- 敏感信息（TMDB Key、assrt Token、115 Cookie）改为 AES-256-GCM 加密存储，密钥独立保存在 `secret.key`（权限 600），旧明文数据首次读取时自动迁移为密文
- 设置接口不再回传完整密钥：只返回掩码（`****后4位`）；前端原样回传掩码时服务端保留原值
- 115 Cookie 从明文文件迁移为加密入库，旧 `115-cookies.txt` 自动迁移后删除

网络适配（QNAP 实测）：
- 115 API 域名（qrcodeapi/webapi/proapi/userapi/passportapi/uplb）支持通过 docker-compose `extra_hosts` 固定到可达 IP（默认网关 IP 被封时可用，详见 DEPLOY-QNAP.md）
- 容器网络建议 `network_mode: host`，绕过 QTS 容器虚拟网关不稳定的问题
- 115 接口请求增加有限重试（二维码获取 5 次、状态轮询容错、登录完成 3 次），对抗家用网络瞬断
- 容器 DNS 可配置公共 DNS（223.5.5.5 / 119.29.29.29），规避运营商/Tailscale 劫持

## v1.1.0 — 2026-07-24

**双通道架构拆分：观影端与管理端分离**

- 新增观影端 `/`：海报墙 + 搜索/过滤 + 播放，默认免密，面向电视/投影仪/iPad
- 新增管理端 `/admin`：115 登录、下载队列、目录配置、密钥设置，独立管理密码
- 新增可选「观影密码」：设置后观影端需输密码，与管理密码互不影响
- 会话增加角色字段（admin/viewer），观影会话无法访问任何管理接口（已做越权测试）
- 观影端仅在管理端已登录时显示「搜索字幕 / 重新匹配」维护按钮
- 扫描媒体库入口从观影页移至管理端设置页

## v1.0.0 — 2026-07-24

**初始版本**

- 115 网盘：网页二维码扫码登录（p115client），网盘浏览/搜索/勾选下载，整文件夹递归加入队列
- 下载器：串行队列、断点续传、暂停/继续/取消、限速，下载完成自动刮削
- 媒体库：目录扫描 + guessit 文件名解析 + TMDB 海报/简介/评分/题材刮削，海报本地缓存
- 字幕：本地同名字幕识别 + assrt.net 在线中文字幕搜刮（.zh.srt）
- 播放：Range 流式播放（支持拖动），设备自解码，NAS 零转码；直链可复制到外部播放器
- Plex 式海报墙：关键字搜索 + 类型/题材/年份过滤，剧集按季分组
- 设置页网页化目录管理：浏览 NAS 目录、新建文件夹、指定电影/剧集/下载目录
- Docker 单容器部署（QNAP Container Station），限制 512MB 内存 / 1 核
